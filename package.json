{
  "name": "extensao-whatsappweb",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node scripts/check-syntax.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}